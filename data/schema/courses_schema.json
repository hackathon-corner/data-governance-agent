{
  "name": "courses",
  "description": "Schema for course catalog.",
  "columns": [
    { "name": "course_id", "type": "string", "required": true },
    { "name": "course_name", "type": "string", "required": true },
    { "name": "instructor", "type": "string", "required": false }
  ]
}
